info:
  name: /me/password
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/me/password"
  body:
    type: json
    data: |-
      {
        "current_password": "password12345",
        "new_password": "password12345-new"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

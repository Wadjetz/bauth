info:
  name: well_known_doc
  type: http
  seq: 18

http:
  method: GET
  url: "{{host}}/.well-known/jwks.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

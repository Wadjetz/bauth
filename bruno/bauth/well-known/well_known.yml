info:
  name: well_known
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/.well-known/oauth-authorization-server"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
